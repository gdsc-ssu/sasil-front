import styled, { css } from '@emotion/native';

export const Wrap = styled.View({
  width: 890,
  height: 208,
  display: 'flex',
  flexDirection: 'row',
  flexWrap: 'wrap',
});

export const item = css({
  margin: 4,
});
