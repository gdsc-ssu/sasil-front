import styled, { css } from '@emotion/native';

export const Container = styled.ScrollView({
  width: '100%',
  display: 'flex',
  flexDirection: 'row',
});

export const item = css({
  margin: 9,
});
