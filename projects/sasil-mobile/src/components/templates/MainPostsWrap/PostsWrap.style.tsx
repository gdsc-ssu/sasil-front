import styled from '@emotion/native';

export const Wrap = styled.View({
  width: 890,
  height: 208,
  display: 'flex',
  flexDirection: 'row',
  flexWrap: 'wrap',
  overflow: 'scroll',
});

export const Item = styled.View({
  margin: 4,
});
