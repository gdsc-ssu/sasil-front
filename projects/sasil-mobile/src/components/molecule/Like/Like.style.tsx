import styled, { css } from '@emotion/native';

export const LikeWrap = styled.View({
  display: 'flex',
  flexDirection: 'row',
  alignItems: 'center',
});

export const labelWrap = css({
  height: 14,
});

export const iconWrap = css({
  width: 14,
  height: 14,
  marginRight: 2,
});
