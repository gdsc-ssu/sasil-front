import styled from '@emotion/native';

export const wrap = styled.View({
  display: 'flex',
  flexDirection: 'row',
  alignContent: 'center',
});

export const textWrap = styled.View({
  marginLeft: 4,
});
